#include "main.h"
#include <limits.h>

/**
 * main - check the code
 *
 * Return: always 0.
*/
int main(void)
{
	int len1;
	int len2;
	unsigned int i;

	len1 = printf("Let's print a simple string !");
	len2 = _printf("Lets's print a simple string !");

	return (0);
}